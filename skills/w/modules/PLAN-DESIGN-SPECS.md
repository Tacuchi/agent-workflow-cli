# PLAN-DESIGN-SPECS — retired

> **DO NOT FOLLOW THIS MODULE.** No loop takes this path any more.

A plan gets its design from [`DESIGN-REFERENCES.md`](DESIGN-REFERENCES.md), not
from a per-screen design SPEC in a session. This tombstone exists so a stale
reference lands somewhere that says so; it is **deleted** in the last phase of
plan 012. No alias, no dual-read, no importer, no migration.
