# BACKLOG.md — deferred items and follow-ups (common)

> What it is: items deferred or surfaced during the session that do not block closure but should not be lost. Written by the loop when `Cerrar` is triggered, **only when there is something to defer or follow up** — no empty BACKLOG. Owned by `refine` and `quick` sessions.

## Deferred (text):
List of phases/tasks (ref: plan-doc `docs/plans/PPP-plan.md`; or `TASKS.md` if the session created its own breakdown) deferred with reason.

## Followups (text):
List of activities / suggestions / dependencies for future follow-up.
