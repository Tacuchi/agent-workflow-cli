# Objective — session001-dev-git-tile-working-branch

## Type
bugfix

## Requirement
En el dashboard TUI (hub mode), el tile GIT muestra como rama de trabajo la rama actual del primer source (ej. 'desarrollo') en lugar de la rama de trabajo DEFINIDA en el hub (## Status > Ramas de trabajo actuales). Debe mostrar siempre la working-branch definida, sin importar en qué rama estén las fuentes.
